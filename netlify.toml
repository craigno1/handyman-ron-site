[build]
  command = "pnpm install && PORT=3000 BASE_PATH=/ pnpm --filter @workspace/web-app run build"
  publish = "artifacts/web-app/dist/public"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
